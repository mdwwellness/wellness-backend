/node_modules

# Env files — ignore real env files, keep the committed example
/.env
/.env.*
!/.env.example

# Graphify generated knowledge graph (regenerate offline with `graphify update .`)
/graphify-out/